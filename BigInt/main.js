"use strict";
/**
 * By using strict mode, JavaScript enforces stricter rules
 * and generates errors for common coding mistakes.
 * This helps in writing cleaner and more robust code
 */

let large_number = Number.MAX_SAFE_INTEGER;
let small_number = Number.MIN_SAFE_INTEGER;

console.log(large_number);
console.log(small_number);
